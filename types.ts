export type Data = {
  id: number;
  icon: string;
  path: string;
}

export interface Contact {
  id: number;
  name: string;
  email?: string;
}

export interface Post {
  id: number;
  title: string;
  body: string;
}

